import { Customer, Equipment, Job, Invoice, Technician } from '@/types';

// Empty technicians array for first-time setup
export const mockTechnicians: Technician[] = [];

// Empty customers array for first-time setup
export const mockCustomers: Customer[] = [];

// Empty equipment array for first-time setup
export const mockEquipment: Equipment[] = [];

// Empty jobs array for first-time setup
export const mockJobs: Job[] = [];

// Empty invoices array for first-time setup
export const mockInvoices: Invoice[] = [];